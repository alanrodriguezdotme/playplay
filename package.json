{
  "name": "playplay",
  "private": true,
  "scripts": {
    "dev": "pnpm -F @playplay/shared build && concurrently \"pnpm -F @playplay/server dev\" \"pnpm -F @playplay/web dev\"",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "setup": "node scripts/setup.mjs",
    "start": "node packages/server/dist/index.js"
  },
  "devDependencies": {
    "@clack/prompts": "^0.9.0",
    "concurrently": "^9.1.2",
    "qrcode-terminal": "^0.12.0",
    "typescript": "^5.7.3"
  }
}