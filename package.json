{
  "name": "playplay-venue",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm -F @playplay/server dev\" \"pnpm -F @playplay/web dev\"",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
